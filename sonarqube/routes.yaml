apiVersion: route.openshift.io/v1
kind: Route
metadata:
  namespace: flux
  labels:
    app: sonarqube
  name: sonarqube
spec:
  to:
    kind: Service
    name: sonarqube
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect